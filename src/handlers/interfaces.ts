export interface ARResult {
  points: string;
  level: string;
  interestLevel: string;
  author: string;
  title: string;
  isbn: any[];
  isExactMatch: boolean;
  searchQuery: string;
  error?: string;
}
